const KakaoSelection = (props) => {
  console.log(props.cityList);
  return (
    <div>
      {/* <ul>
        {props.cityList.length === 0 ? (
          <div>Sorry. Not Found</div>
        ) : (
          props.cityList.map((city) => <span>city.name</span>)
        )}
      </ul> */}
    </div>
  );
};

export default KakaoSelection;
